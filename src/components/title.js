import React from "react";
import { Element, Link } from "react-scroll";
import "./title.css"

const Title = () => {
  return (
    <div name="titleSection" className="title">
      {/* Add another component or content for section 2 */}
      <h1>Title</h1>
    </div>
  );
};

export default Title;
